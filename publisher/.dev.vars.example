# Local Publisher (wrangler dev) environment variables
# Copy to publisher/.dev.vars and fill secrets for local dev (never commit .dev.vars)

BASE_URL=http://localhost:8788
ADMIN_GITHUB_LOGINS=<your-github-login>
CONTENT_REPO=<owner>/<repo> # e.g. yourname/yourname.github.io
CONTENT_BRANCH=main
CONTENT_ROOT=
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:4173,https://<user>.github.io
TOKEN_TTL_SECONDS=43200

# Secrets (required)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
TOKEN_SECRET=

